import MainLayout from "./MainLayout"

/**
 * AdminLayout wrapper component.
 * Currently uses MainLayout as the primary visual shell.
 * Can be customized here for admin-specific side-effects, providers, or layout changes.
 */
export function AdminLayout() {
  return <MainLayout />
}

export default AdminLayout
